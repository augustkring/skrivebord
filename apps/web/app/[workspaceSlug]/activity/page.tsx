import { buildAlsLebenSnapshot } from "@skrivebord/domain";
import { PageTitle } from "../_components/page-title";
export default function ActivityPage() { const {activity}=buildAlsLebenSnapshot(); return <><PageTitle title="Aktivitet" subtitle="Menneskeligt læsbar driftshistorik. Tekniske detaljer hører til i audit/inspector, ikke i standardlisten."/><div className="border-t border-[var(--border-default)]">{activity.map((event)=><article key={event.id} className="grid gap-2 border-b border-[var(--border-default)] py-4 sm:grid-cols-[72px_1fr]"><time className="text-sm text-[var(--text-muted)]">{event.timeLabel}</time><div><div className="font-medium">{event.summary}</div>{event.detail ? <div className="mt-1 text-sm text-[var(--text-secondary)]">{event.detail}</div> : null}</div></article>)}</div></>; }
