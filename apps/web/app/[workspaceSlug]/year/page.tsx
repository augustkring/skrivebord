import { buildAlsLebenSnapshot } from "@skrivebord/domain";
import { PageTitle } from "../_components/page-title";

const months = ["Januar","Februar","Marts","April","Maj","Juni","Juli","August","September","Oktober","November","December"];
export default function YearPage() { const { yearPlanItems } = buildAlsLebenSnapshot(); return <><PageTitle title="År" subtitle="Tilbagevendende og sæsonbestemt arbejde uden dekorativt årshjul. September er åbnet, fordi det er den aktuelle måned i pilotdata."/><div className="space-y-2">{months.map((month,index) => { const items=yearPlanItems.filter((item)=>item.month===index+1); const current=index===8; return <section key={month} className={`rounded-lg border ${current ? "border-[var(--border-strong)] bg-white" : "border-[var(--border-default)]"}`}><div className="flex items-center justify-between px-4 py-3"><h2 className="font-semibold">{month}</h2><span className="text-sm text-[var(--text-muted)]">{items.length ? `${items.length} aktivitet` : "Ingen aktiviteter"}</span></div>{current && items.map((item)=><div key={item.id} className="border-t border-[var(--border-default)] px-4 py-4"><div className="font-medium">{item.title}</div><div className="mt-1 text-sm text-[var(--text-secondary)]">{item.description}</div><div className="mt-2 text-xs text-[var(--text-muted)]">Vindue: {item.windowLabel}</div></div>)}</section>; })}</div></>; }
