import { buildAlsLebenSnapshot, buildTodayView } from "@skrivebord/domain";
import { PageTitle } from "../_components/page-title";
export default function AttentionPage() { const view=buildTodayView(buildAlsLebenSnapshot(),new Date("2026-09-22T08:00:00+02:00")); return <><PageTitle title="Opmærksomhed" subtitle="Kun arbejde hvor et menneske faktisk er nødvendigt. Ikke en generel notifikationsfeed."/><div className="border-y border-[var(--border-default)]">{view.requiresYou.map((item)=><article key={item.id} className="border-b border-[var(--border-default)] py-5 last:border-b-0"><div className="mb-2 text-xs font-semibold uppercase tracking-[0.12em] text-[var(--status-warning)]">{item.attentionType ?? "Opmærksomhed"}</div><h2 className="font-semibold">{item.title}</h2><p className="mt-1 max-w-2xl text-sm leading-6 text-[var(--text-secondary)]">{item.reason}</p><button className="mt-4 rounded-md bg-[var(--action-primary)] px-3 py-2 text-sm font-semibold text-white">{item.recommendedActionLabel}</button></article>)}</div></>; }
